- name: conditions
  hosts: local
  connection: local
  vars:
    live_sessions: 6
  tasks:
  - name: is ansible more than  5 sessions
    debug:
      msg: "yes, ansible is more than 5 sessions"
    when: live_sessions >2

